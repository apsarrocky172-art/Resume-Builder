// React plugin is disabled because '@vitejs/plugin-react' is not installed.
// Install the package and restore this import if React-specific Vite behavior is needed.
// import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default {
  plugins: [],
  server: {
    port: 3000,
    host: true
  }
};
